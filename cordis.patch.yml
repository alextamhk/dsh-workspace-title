# harness-workspace-title bundle patch: inserts its plugin row into the web
# plugin roster. The single row mounts BOTH halves — the Host side (which only
# supplies the Config schema the settings mirror reads) and the browser side
# (served from this package's lib/client.js).
- insert:
    - id: harness-workspace-title
      name: 'harness-workspace-title'
